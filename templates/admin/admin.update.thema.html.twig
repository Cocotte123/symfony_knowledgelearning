{% extends 'base.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}
<main  class="container mx-auto p-2 text-center" style="width: 90%;">

  <section class=" container text-center mt-5 mb-5" style="color:#00497c;">
    <h1>Bonjour {{ admin.firstName }} {{ admin.lastName}}</h1>
    <a href="{{path('app_admin_learnings')}}" class="btn" style="width: 200px;border:solid #384050 1px;background-color:#384050; color:#f1f8fc;" aria-label="Formations">Formations</a>
  </section>

    
    <section class="container mx-auto p-2 text-center" style="width: 80%;border:solid black 1px;">
    <h2 class="mb-5 text-decoration-underline">Modifier le thème {{modifiedThema.name}}</h2>
    {{ form_start(modifyThemaForm) }}
    <div  class="row">
 
            <div class="col-md-6">
            {{ form_row(modifyThemaForm.name) }}
            </div>
            <div class="col-md-6  my-auto">
            {{ form_row(modifyThemaForm.update) }}
            </div>
     {{ form_end(modifyThemaForm) }}
   </section>
    
</main>
{% endblock %}
